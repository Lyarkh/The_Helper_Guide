- **Funcionamento**: Uma árvore binária onde o pai é sempre maior (max heap) ou menor (min heap) que seus filhos.
- **Complexidade**: `O(log n)` para inserção e remoção.
- **Resumo**: Útil para implementar filas de prioridade.